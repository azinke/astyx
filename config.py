"""Configuration module."""

DATASET_ROOT_DIR: str = "dataset_astyx_hires2019"

# Note that the path here point to the entry point of the astyx dataset
# From that enty point (dataset.json), all the dataset can be inspected
DATASET: str = DATASET_ROOT_DIR + "/dataset.json"
